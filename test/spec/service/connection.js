'use strict';

describe('Serivce: phoneBook', function () {
    var rootScope, unStreamConnectionService;
    beforeEach(module('unchatbar-stream'));


    beforeEach(inject(function ($rootScope, unStreamConnection) {
        rootScope = $rootScope;
        unStreamConnectionService = unStreamConnection;
    }));

    describe('check methode', function () {


    });
});